package com.ethan.voxyworldgenv2.mixin;

// MC 1.18.2 MinecraftServer has no `emptyTicks` field (the idle-pause counter was added later), so the
// original accessor has no valid target. Retained as an empty placeholder; no longer referenced.
public interface MinecraftServerAccess {
}
